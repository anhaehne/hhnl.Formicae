# Formicae Workflow

- Workflow: `cac05562609f4499b9606d43185708b2`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/39
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text
## Changes from previous plan

- Added explicit architecture coverage for third-party and internal integrations that can contribute workflow elements.
- Added a plugin/extension model for custom tasks, triggers, environments, capabilities, MCP servers, images, and tools.
- Added a future natural-language workflow authoring path where Formicae chat can create or modify workflow elements.
- Added validation and review steps for generated/custom workflow elements.
- Kept scope docs-only: this issue delivers architecture documentation, not runtime implementation.

## Implementation Plan

1. Confirm repository baseline.
   - Read `README.md`.
   - Read `docs/mvp-workflow.md`.
   - Skim:
     - `src/hhnl.Formicae.Application/Workflows/WorkflowModels.cs`
     - `WorkflowOrchestrator.cs`
     - `Interfaces.cs`
     - `src/hhnl.Formicae.Api/prompts/*.md`
   - Summarize the current hardcoded MVP workflow: `Plan -> Implement -> CreatePullRequest -> AddressComments`.

2. Research workflow prior art.
   - Review GitHub Actions, Argo Workflows, Temporal, Airflow DAGs, Tekton, Buildkite, and typed config approaches such as CUE/Dhall/Nix where relevant.
   - Compare readability, validation, branching, loops, parallelism, versioning, secret handling, extension model, and runtime complexity.
   - Capture findings in the new architecture doc, not as separate notes.

3. Create `docs/configurable-workflows-architecture.md`.
   - Mark it clearly as planned architecture.
   - Include goals, non-goals, current MVP relationship, and open decisions.

4. Document DSL options.
   - Compare YAML/JSON declarative DSL, code-defined workflows, embedded engines, and typed schema-first DSLs.
   - Recommend an initial direction.
   - Clearly separate example shapes from committed schema.

5. Define the proposed core model.
   - Include `WorkflowDefinition`, `WorkflowDefinitionVersion`, `WorkflowRun`, `WorkflowStep`, `StepRun`, `WorkflowRunEvent`, `TaskDefinition`, `TriggerDefinition`, `EnvironmentDefinition`, `Persona`, `CapabilitySet`, and `SecretReference`.
   - State ownership boundaries: Application owns orchestration concepts; Infrastructure resolves GitHub, Kubernetes, secrets, MCP, images, tools, and runner details.

6. Add integration and extension architecture.
   - Explain how integrations can provide custom workflow elements:
     - tasks
     - triggers
     - environments
     - capabilities
     - secret reference types
     - MCP server definitions
     - tool installers
     - container images
   - Define required metadata for contributed elements: stable id, version, schema, display name, validation rules, required capabilities, secret requirements, and compatibility constraints.
   - Describe registration/discovery at startup or via configured providers.
   - Require all integration-provided elements to pass schema validation before they can be referenced by a workflow.

7. Add future natural-language authoring architecture.
   - Describe how a future Formicae chat integration could create or modify workflow definitions and custom elements from user prompts.
   - Include a safe flow:
     - user describes desired workflow or element
     - agent proposes structured definition
     - validator checks schema, references, permissions, secrets, loop bounds, and deterministic conditions
     - user reviews diff
     - approved definition is saved as a new version
   - State that natural-language generation must never directly execute unreviewed workflow changes.
   - Include examples for creating a reusable task, adding a trigger, and changing a workflow branch.

8. Define DSL examples.
   - Include compact examples for:
     - sequential workflow
     - decision branch
     - bounded loop
     - parallel branches
     - script step
     - reusable task
     - per-step persona
     - per-step environment
     - per-step secrets
     - integration-provided task
     - integration-provided trigger

9. Document execution semantics.
   - Cover sequential execution, deterministic decisions, bounded loops, parallel aggregation, script versus agent steps, retries, idempotency, cancellation, failure handling, and audit events.

10. Add upgradeability/versioning.
   - Version workflow definitions and DSL schema.
   - Persist selected definition/version on every `WorkflowRun`.
   - Define compatibility rules for in-flight runs.
   - Cover migrations, defaulting, deprecation, and integration element version compatibility.
   - Include an example where an older workflow definition still loads after a schema change.

11. Map issues #9-#22.
   - #9 loops
   - #10 parallel execution
   - #11 decisions/branches
   - #12 scriptable steps
   - #13 triggers
   - #14 personas
   - #15 custom tasks
   - #16 capabilities
   - #17 per-step environments
   - #18 per-step secrets
   - #19 customizable environments
   - #20 MCP servers
   - #21 custom images
   - #22 tool installs
   - Mark each as supported, deferred, or open.

12. Add security and validation rules.
   - Reject unknown step, task, trigger, environment, persona, capability, MCP, image, tool, and secret references.
   - Require bounded loops.
   - Require deterministic branch conditions.
   - Validate integration-provided schemas before execution.
   - Redact secrets from API responses, logs, events, prompts, and UI.
   - Fail before Kubernetes job creation when configuration is invalid.

13. Add Mermaid architecture diagram.
   - Show trigger event, definition/version resolution, run creation, scheduler, runtime resolution, agent/script execution, events, and next-step evaluation.
   - Include integration registry and validation path.
   - Include natural-language authoring as a future side path that produces reviewed workflow definition versions.

14. Run critical review with a subagent.
   - Ask it to challenge DSL choice, extension model, natural-language authoring safety, upgrade/versioning, security boundaries, and #9-#22 coverage.
   - Add a short `Review Feedback Incorporated` or `Open Objections` section to the document.

15. Add migration strategy.
   - Keep the current hardcoded MVP workflow as the built-in default.
   - Introduce workflow definitions read-only first.
   - Persist definition/version on each run.
   - Later move `Plan`, `Implement`, `CreatePullRequest`, and `AddressComments` into built-in task definitions.
   - Defer editing UI, database migrations, and full runtime implementation.

16. Update existing docs.
   - Link from `docs/mvp-workflow.md` to the new architecture doc.
   - Link from `README.md` only if there is a natural architecture/docs section.
   - Avoid changing current MVP behavior descriptions except to clarify configurable workflows are planned.

17. Verify docs-only change.
   - Check relative links manually.
   - Run markdown/spell checks if the repo has them.
   - Otherwise run:
     ```bash
     rg "configurable-workflows-architecture|MVP Workflow|workflow" docs README.md
     ```
   - Do not run `dotnet test` unless code changes were made.

18. Final acceptance checklist.
   - New architecture doc exists under `docs/`.
   - Prior art and DSL tradeoffs are documented.
   - Integration-provided workflow elements are covered.
   - Future chat-based natural-language authoring is covered.
   - Every linked issue #9-#22 is mapped.
   - MVP workflow maps to the proposed configurable model.
   - Upgradeability/versioning is explicit.
   - Security, validation, auditability, and secret handling are covered.
   - Critical subagent feedback is summarized or addressed.
   - Existing docs link to the new document.
```

### Implement

Status: Succeeded

```text
Implemented the docs-only workflow architecture plan.

Changed:
- Added [docs/configurable-workflows-architecture.md](/workspace/repo/docs/configurable-workflows-architecture.md) with prior-art links, DSL tradeoffs, core model, extension model, natural-language authoring, examples, execution semantics, versioning, #9-#22 mapping, security rules, Mermaid diagram, migration strategy, and incorporated subagent review feedback.
- Linked it from [docs/mvp-workflow.md](/workspace/repo/docs/mvp-workflow.md) and [README.md](/workspace/repo/README.md).
- Bumped aligned version references to `0.3.24` in [Directory.Build.props](/workspace/repo/Directory.Build.props), Helm chart/values, and deployment docs.

Verification:
- Confirmed relative doc links exist.
- Ran requested `rg "configurable-workflows-architecture|MVP Workflow|workflow" docs README.md`.
- Checked version alignment with `rg "0\\.3\\.23|0\\.3\\.24|0\\.3\\.18"`.
- Ran `git diff --check`.
- No `dotnet test` run because this was docs-only; no markdown/spellcheck script exists in the repo.
```

### CreatePullRequest

Status: Running

```text

```
