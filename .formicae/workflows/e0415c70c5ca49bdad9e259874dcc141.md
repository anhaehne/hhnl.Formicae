# Formicae Workflow

- Workflow: `e0415c70c5ca49bdad9e259874dcc141`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/54
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text
## Implementation Plan

1. Reproduce the issue with a failing regression test.
   - Add a test in `tests/hhnl.Formicae.Tests/WorkflowOrchestratorTests.cs` that creates two connected repositories, labels one issue in each repo with `ready-to-plan`, runs `WorkflowDiscoveryService.DiscoverReadyToPlanWorkflowsAsync`, and asserts:
     - `discovered == 2`
     - two workflows are created
     - each workflow keeps the correct `RepositoryUrl` and `BaseBranch`
     - `ListIssuesWithLabelAsync` was called for both repositories
   - There is already similar coverage, so first run it alone. If it passes, add a higher-level test around `WorkflowBackgroundService` or the exact production service registration path to catch DI/config behavior.

2. Inspect production registration/config.
   - Check `src/hhnl.Formicae.Api/Program.cs` and `src/hhnl.Formicae.Infrastructure/DependencyInjection.cs`.
   - Confirm production uses `DevOpsWorkItemProvider` plus `IDevOpsIntegrationStore.ListAllRepositoriesAsync`, not a single `WorkflowDiscovery:RepositoryUrl` fallback.
   - Confirm `WorkflowDiscoveryService` is constructed with `integrationStore` in real DI.

3. Fix the smallest failing layer.
   - If DI omits `IDevOpsIntegrationStore`, update service registration so `WorkflowDiscoveryService` receives it.
   - If repository discovery only uses `WorkflowDiscoveryOptions.RepositoryUrl`, update `WorkflowDiscoveryService.GetDiscoveryRepositoriesAsync` to merge all connected repositories from `integrationStore.ListAllRepositoriesAsync`.
   - Keep the existing single `RepositoryUrl` config as a backward-compatible fallback.
   - Preserve case-insensitive de-duplication by normalized repository URL.

4. Add failure isolation per repository.
   - In `WorkflowDiscoveryService.DiscoverReadyToPlanWorkflowsAsync`, wrap each repository’s `ListIssuesWithLabelAsync` processing so one bad repository does not prevent later repositories from being scanned.
   - Log a warning for the failed repository if logging is available; otherwise add a workflow/store log only when a workflow context exists.
   - Add a test where the first repository throws and the second still queues a workflow.

5. Verify provider resolution for each repository.
   - Add or extend tests for `DevOpsPlatformFactory.CreateForRepositoryAsync` with two connected repositories and, if practical, mixed GitHub/Gitea repositories.
   - Assert each repository URL resolves to its own connected repository/integration, not the first one.

6. Run validation.
   - `dotnet test tests/hhnl.Formicae.Tests/hhnl.Formicae.Tests.csproj`
   - If frontend or config docs are touched, also run the relevant existing build command from `README.md`.

7. Update docs only if behavior/config changed.
   - If the fix changes discovery configuration, update `README.md` and `docs/kubernetes-deployment.md` to state that background discovery scans all connected repositories, while `WorkflowDiscovery:RepositoryUrl` is only a legacy/manual fallback.
```

### Implement

Status: Succeeded

```text
Implemented the workflow discovery fix.

Changed:
- [WorkflowDiscoveryService.cs](/workspace/repo/src/hhnl.Formicae.Application/Workflows/WorkflowDiscoveryService.cs:33): repository scans are now isolated per repository; non-cancellation failures log a warning and discovery continues.
- [hhnl.Formicae.Application.csproj](/workspace/repo/src/hhnl.Formicae.Application/hhnl.Formicae.Application.csproj:10): added logging abstractions for `ILogger<T>`.
- [WorkflowOrchestratorTests.cs](/workspace/repo/tests/hhnl.Formicae.Tests/WorkflowOrchestratorTests.cs:107): added failure-isolation regression coverage.
- [WorkflowOrchestratorTests.cs](/workspace/repo/tests/hhnl.Formicae.Tests/WorkflowOrchestratorTests.cs:2443): added production-style DI registration coverage for connected repository discovery.
- [DevOpsPlatformFactoryTests.cs](/workspace/repo/tests/hhnl.Formicae.Tests/DevOpsPlatformFactoryTests.cs:72): added mixed GitHub/Gitea provider resolution coverage.
- [MockDevOpsAdapter.cs](/workspace/repo/tests/hhnl.Formicae.Tests/TestDoubles/MockDevOpsAdapter.cs:28): added per-repository list failure support for tests.

Tests added/removed/edited: 3 added, 0 removed, 1 edited test double.

Validation:
- `dotnet test tests/hhnl.Formicae.Tests/hhnl.Formicae.Tests.csproj`
- Passed: 185 / Failed: 0 / Skipped: 0

Docs were not changed because discovery configuration behavior did not change; the existing connected-repository scan path was already present and preserved.
```

### CreatePullRequest

Status: Running

```text

```
