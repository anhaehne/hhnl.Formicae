# Formicae Workflow

- Workflow: `4dc7ffa1a77642e1a523c1915736ee02`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/49
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text
## Changes from previous plan

- Replace the proposed `IForgeApi` abstraction with `IDevOpsPlatform`, per issue feedback.
- Keep `IWorkItemProvider` and `ISourceControlProvider` as public workflow-facing interfaces; introduce `IDevOpsPlatform` underneath them for GitHub/Gitea shared behavior.
- Split webhook work into a shared processor plus GitHub/Gitea request adapters to avoid duplicating workflow state transitions.

## Implementation Plan

1. **Baseline and inventory**
   - Run `dotnet test hhnl.Formicae.slnx`.
   - From `src/hhnl.Formicae.Api/ClientApp`, run `npm ci` if needed, then `npm run build`.
   - Record any existing failures before code changes.
   - Review current GitHub surfaces:
     - `src/hhnl.Formicae.Infrastructure/GitHub/*`
     - `src/hhnl.Formicae.Application/Integrations/*`
     - `src/hhnl.Formicae.Api/GitHubWebhookHandler.cs`
     - integration API routes in `src/hhnl.Formicae.Api/Program.cs`.

2. **Add provider-neutral integration metadata**
   - Add `Gitea` to `DevOpsProviderType`.
   - Extend `DevOpsIntegration` with provider-neutral fields:
     - `ServerUrl`
     - `AccessToken`
     - keep existing GitHub fields for compatibility.
   - Rename response concepts where practical without breaking existing clients:
     - keep GitHub fields, but add neutral fields such as `ProviderClientId`, `SetupInstructions`, `RequiredWebhookEvents`.
   - Add EF migration and update:
     - `FormicaeDbContext`
     - `EfDevOpsIntegrationStore`
     - `InMemoryDevOpsIntegrationStore`.
   - Tests:
     - create/update GitHub still works.
     - create/update Gitea stores server URL, token, webhook secret.
     - existing GitHub integration tests remain green.

3. **Extract repository and work-item URL parsing**
   - Add provider-neutral references, for example:
     - `DevOpsRepositoryReference`
     - `DevOpsIssueReference`
     - `DevOpsPullRequestReference`.
   - Support:
     - GitHub repository: `https://github.com/{owner}/{repo}`.
     - GitHub issue/PR URLs.
     - Gitea repository: `{serverUrl}/{owner}/{repo}` with optional `.git`.
     - Gitea issue/PR URLs using Gitea’s URL shapes.
   - Replace duplicated GitHub parsers in:
     - `DevOpsIntegrationService`
     - `GitHubWorkItemProvider`
     - `GitHubSourceControlProvider`
     - `GitHubClientFactory`.
   - Tests:
     - valid GitHub repo/issue/PR.
     - valid Gitea repo/issue/PR.
     - `.git` suffix normalization.
     - invalid host for selected provider.
     - issue URL rejected where repo URL is expected.

4. **Introduce `IDevOpsPlatform`**
   - Add `IDevOpsPlatform` in application or infrastructure boundary, depending on existing layering.
   - Shape it around provider-neutral operations already needed by workflows:
     - get issue
     - list labeled issues
     - list/create/update issue comments
     - react to issue/comment where supported
     - get branch ref
     - create branch/ref
     - create/list/get PR
     - list PR comments/review comments
     - create/update repository file
   - Add provider-neutral DTOs, avoiding Octokit types in shared code.
   - Implement `GitHubDevOpsPlatform` by adapting current `OctokitGitHubApi`.
   - Tests:
     - GitHub provider behavior still passes through `IDevOpsPlatform`.
     - unsupported optional operations are explicit no-ops or throw clear provider-specific exceptions only where workflow cannot continue.

5. **Move shared workflow provider logic above platforms**
   - Create shared implementations, for example:
     - `DevOpsWorkItemProvider`
     - `DevOpsSourceControlProvider`.
   - These use `IDevOpsPlatformFactory` and provider-neutral URL references.
   - Move common logic from:
     - `GitHubWorkItemProvider`
     - `GitHubSourceControlProvider`.
   - Keep GitHub-specific details only in `GitHubDevOpsPlatform`.
   - Tests:
     - existing GitHub work-item tests.
     - existing GitHub source-control tests.
     - branch already exists behavior.
     - workflow summary file create/update.
     - PR reuse when one already exists.

6. **Implement Gitea platform client**
   - Add `src/hhnl.Formicae.Infrastructure/Gitea`.
   - Implement `GiteaDevOpsPlatform` using `HttpClient`.
   - Use token auth from the matching `DevOpsIntegration.AccessToken`.
   - Implement required Gitea REST calls:
     - get issue
     - list issues by label
     - list/create/edit issue comments
     - get branch/ref
     - create branch from base SHA/ref
     - create/list/get pull requests
     - list PR comments where Gitea exposes them
     - create/update file contents.
   - For GitHub-only linked branch behavior, create a normal Gitea branch from the base ref.
   - If reactions are unavailable or not equivalent, make reactions no-op with logging so workflows continue.
   - Tests with fake `HttpMessageHandler`:
     - auth header applied.
     - URL construction honors custom server URL.
     - issue retrieval.
     - comment upsert.
     - branch creation.
     - file create/update.
     - PR create and existing PR reuse.
     - unsupported reaction no-op.

7. **Add platform dispatch**
   - Add `IDevOpsPlatformFactory`.
   - Resolve platform by connected repository URL and integration provider.
   - GitHub repositories use GitHub App installation token flow.
   - Gitea repositories use stored server URL and access token.
   - Replace current non-fake DI registration in `DependencyInjection.cs` with shared providers plus platform factory.
   - Preserve:
     - `UseFakeAdapters=true`
     - `WorkItemMode=Fake`
     - `SourceControlMode=Fake`.
   - Tests:
     - GitHub URL dispatches to GitHub platform.
     - Gitea URL dispatches to Gitea platform.
     - unknown/unconnected repo gives a clear error.

8. **Add Gitea integration API**
   - Add request records:
     - `CreateGiteaIntegrationRequest`
     - `UpdateGiteaIntegrationRequest`.
   - Add routes:
     - `POST /api/integrations/gitea`
     - `PUT /api/integrations/{integrationId}/gitea`.
   - Update repository add flow:
     - GitHub still requires `InstallationId`.
     - Gitea does not require `InstallationId`.
     - Gitea repository URL must match the integration `ServerUrl`.
   - Add optional repository listing endpoint for Gitea if token supports it; otherwise allow manual repository URL entry.
   - Tests:
     - create Gitea integration.
     - update Gitea token/server URL.
     - add Gitea repository.
     - reject duplicate repository.
     - reject Gitea repository outside configured server.
     - GitHub validation still requires installation ID.

9. **Refactor and add webhook support**
   - Extract shared webhook workflow behavior from `GitHubWebhookHandler`:
     - signature verification helper.
     - event/action decision.
     - workflow tick signaling.
     - complete workflow when PR merged.
     - requeue completed workflow on PR comments/reviews.
   - Keep provider-specific request adapters:
     - `GitHubWebhookHandler`
     - `GiteaWebhookHandler`.
   - Add route:
     - `POST /api/webhooks/gitea`.
   - Verify Gitea signatures using:
     - `X-Gitea-Signature`
     - also support `X-Hub-Signature-256` where compatible.
   - Tests:
     - valid Gitea signature accepted.
     - invalid signature rejected.
     - ping accepted.
     - issue event signals tick.
     - merged PR completes workflow.
     - PR comment requeues completed workflow.
     - existing GitHub webhook tests remain green.

10. **Update React client**
   - Update `src/hhnl.Formicae.Api/ClientApp/src/api.ts` types and API functions.
   - Update integration UI in `App.tsx`:
     - provider selector: GitHub or Gitea.
     - GitHub fields remain unchanged.
     - Gitea fields: display name, server URL, access token, webhook secret.
     - setup instructions show provider-specific webhook URL, secret, and required events.
   - Make repository management labels provider-neutral.
   - Tests/checks:
     - `npm run build`.
     - manually verify create GitHub and create Gitea forms render without type errors.

11. **Update Kubernetes and E2E test setup**
   - Add Gitea to `deploy/kubernetes/overlays/e2e`.
   - Seed:
     - Gitea admin/user.
     - access token.
     - test repository.
     - test issue.
   - Configure Formicae in E2E to use real provider dispatch with Gitea.
   - Add an E2E workflow test:
     - create Gitea integration.
     - connect seeded repository.
     - start workflow from Gitea issue URL.
     - verify branch creation.
     - verify `.formicae/workflows/{id}.md`.
     - verify PR creation.
     - verify workflow state persisted.
   - Add webhook E2E or integration-level test:
     - send signed Gitea PR merged payload.
     - verify workflow completes.
     - send signed PR comment payload.
     - verify completed workflow requeues.
   - Keep existing fake Kubernetes workflow test.

12. **Docs and deployment config**
   - Update `docs/kubernetes-deployment.md` with:
     - Gitea server URL.
     - token permissions.
     - webhook URL.
     - webhook secret.
     - required events.
     - signature headers.
   - Update sample Kubernetes and Helm values/secrets with Gitea placeholders.
   - Ensure docs distinguish GitHub App setup from Gitea token setup.

13. **Final verification**
   - Run `dotnet test hhnl.Formicae.slnx`.
   - Run frontend build from `src/hhnl.Formicae.Api/ClientApp`.
   - Run `kubectl kustomize deploy/kubernetes/overlays/e2e`.
   - Run Kubernetes E2E tests when `kind`, `kubectl`, and container runtime are available.
   - Manually verify:
     - `/api/webhooks/github` rejects bad signatures and accepts valid ones.
     - `/api/webhooks/gitea` rejects bad signatures and accepts valid ones.
     - GitHub workflows still work through the new shared `IDevOpsPlatform` path.
     - Gitea workflows complete the full issue-to-PR path.
```

### Implement

Status: Succeeded

```text
Implemented the Gitea integration path and shared DevOps abstraction.

Changed areas:
- Added provider-neutral references and `IDevOpsPlatform` contracts.
- Added shared work item/source control providers and platform dispatch.
- Adapted GitHub through `GitHubDevOpsPlatform`.
- Added `GiteaDevOpsPlatform` with token auth, custom server URL support, issues, comments, branches, files, PRs, and reaction no-ops.
- Added Gitea integration API/routes and UI provider selector/manual repository entry.
- Added shared webhook workflow processor plus Gitea webhook handler/signature verification.
- Added EF migration for `ServerUrl` and `AccessToken`.
- Updated docs, Helm/Kubernetes placeholders, E2E overlay Gitea service/deployment, and bumped version to `0.5.0`.
- Confirmed issue scope from GitHub issue #49: https://github.com/anhaehne/hhnl.Formicae/issues/49

Verification:
- `dotnet test hhnl.Formicae.slnx` passed: `173/173`
- `npm run build` passed
- `git diff --check` passed
- Conflict marker scan passed
- `kubectl kustomize deploy/kubernetes/overlays/e2e` could not run because `kubectl` is not installed. `kind`, container runtime, and `helm` were also unavailable, so full Kubernetes E2E execution was not possible here.
```

### CreatePullRequest

Status: Running

```text

```
