# Formicae Workflow

- Workflow: `863f5c7b61a24dc79e046ceac720324e`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/56
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text
## Implementation Plan

1. Update the planning prompt in `src/hhnl.Formicae.Api/prompts/plan.md`.
   Add an explicit instruction that the planning agent must inspect the checked-out repository code before producing the plan, and should reference concrete files/components where useful.

2. Make plan tasks receive repository checkout credentials.
   In `src/hhnl.Formicae.Infrastructure/OpenHands/OpenHandsAgentRunner.cs`, update `CreateGitAccessTokenAsync` so `TaskRunKind.Plan` is treated as repository work alongside `Implement` and `AddressComments`.

3. Make the worker check out code for plan tasks.
   In `src/hhnl.Formicae.Worker/Program.cs`:
   - Change `RequiresRepositoryCheckout` to include `Plan`.
   - Refactor checkout into a shared helper used before running either Codex or OpenHands.
   - Keep commit/push behavior limited to mutating task kinds: `Implement` and `AddressComments`.
   - For `Plan`, check out `FORMICAE_BRANCH` but return after the agent completes without staging, committing, or pushing changes.

4. Ensure OpenHands runs inside the checked-out repository too.
   When `RequiresRepositoryCheckout` is true, pass `/workspace/repo` as the working directory to the `openhands --headless ...` process instead of `null`.

5. Add focused tests in `tests/hhnl.Formicae.Tests/WorkflowOrchestratorTests.cs`.
   Cover:
   - `OpenHandsAgentRunner` injects `FORMICAE_GIT_ACCESS_TOKEN` for `TaskRunKind.Plan`.
   - Plan job specs still include `FORMICAE_REPOSITORY_URL` and `FORMICAE_BRANCH`.
   - Existing implement/address-comment token behavior still passes.

6. Add or adjust worker-level tests if the project already has a worker command harness.
   Verify:
   - `Plan` clones and checks out the requested branch.
   - `Plan` does not run `git add`, `git commit`, or `git push`.
   - `Implement` and `AddressComments` still clone and push when changes exist.

7. Run verification.
   Execute:
   ```bash
   dotnet test hhnl.Formicae.slnx
   ```
   Also manually inspect the generated plan prompt output path if needed to confirm the new code-inspection instruction is present.
```

### Implement

Status: Succeeded

```text
Implemented the workflow changes.

Changed:
- [plan.md](/workspace/repo/src/hhnl.Formicae.Api/prompts/plan.md): planning prompt now requires inspecting checked-out code and referencing concrete files/components where useful.
- [OpenHandsAgentRunner.cs](/workspace/repo/src/hhnl.Formicae.Infrastructure/OpenHands/OpenHandsAgentRunner.cs): `Plan` now receives repository Git credentials.
- [Program.cs](/workspace/repo/src/hhnl.Formicae.Worker/Program.cs): `Plan` now clones/checks out the repo, OpenHands runs from `/workspace/repo` when checkout is required, and commit/push remains limited to `Implement` and `AddressComments`.
- [WorkflowOrchestratorTests.cs](/workspace/repo/tests/hhnl.Formicae.Tests/WorkflowOrchestratorTests.cs): expanded OpenHands job spec/token coverage.

Tests added/removed/edited: 0 added, 0 removed, 2 edited.

Verification:
- `dotnet test hhnl.Formicae.slnx` passed: 185 passed, 0 failed.
- `git diff --check` passed.
```

### CreatePullRequest

Status: Running

```text

```
