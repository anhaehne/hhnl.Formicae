# Formicae Workflow

- Workflow: `668cac751c8c4ce1a92043cb0fbef13c`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/3
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text
## Changes from previous plan

- Added explicit support for issue chat messages from GitHub comments.
- Added default-collapsed overflow handling for chat bodies, logs, task output, event details, and agent messages.
- Added “individual agent messages where possible” by introducing optional parsed agent message DTOs alongside raw output.
- Tightened the order into small slices: models/store, persistence, clock, event emission, signals, API, then UI and tests.

## Implementation Plan

1. Add observability domain models in `src/hhnl.Formicae.Application/Workflows/WorkflowModels.cs`.
   - Add `WorkflowEvent` with `Id`, `WorkflowId`, `TaskRunId?`, `Type`, `Level`, `Message`, `DetailsJson?`, `CreatedAt`.
   - Add `StartedAt?` and `CompletedAt?` to `TaskRun`.
   - Add response records: `WorkflowEventResponse`, `TaskRunResponse`, `WorkflowSignalResponse`, `WorkflowChatMessageResponse`, and optionally `AgentMessageResponse`.
   - Add event type constants or enum values for queued, transitions, task started/succeeded/failed, external job assigned, PR created, workflow completed/failed, chat captured.

2. Extend mapping in `WorkflowMapping.cs`.
   - Map workflows as-is.
   - Add `TaskRun.ToResponse()` including timestamps and optional parsed agent messages.
   - Add `WorkflowEvent.ToResponse()`.
   - Keep persistence models separate from API response shape.

3. Extend `IWorkflowStore` in `Interfaces.cs`.
   - Add `AddEventAsync(WorkflowEvent evt, CancellationToken)`.
   - Add `ListEventsAsync(Guid workflowId, CancellationToken)`.
   - Add `ListNonTerminalWorkflowsAsync(CancellationToken)` or reuse `ListRunnableWorkflowsAsync` for stale signal detection.
   - Keep existing log methods unchanged for compatibility.

4. Persist observability data.
   - Add `DbSet<WorkflowEvent>` in `FormicaeDbContext`.
   - Map `workflow_events` with required `WorkflowId`, `Type`, `Level`, `Message`; nullable `TaskRunId`, `DetailsJson`.
   - Add indexes on `WorkflowId` and `(WorkflowId, CreatedAt)`.
   - Add nullable `StartedAt` and `CompletedAt` columns to `task_runs`.
   - Update `EfWorkflowStore` and `InMemoryWorkflowStore`.
   - Add EF migration under `src/hhnl.Formicae.Infrastructure/Persistence/Migrations`.

5. Add deterministic time.
   - Add `IClock` with `DateTimeOffset UtcNow`.
   - Add production `SystemClock`.
   - Register it in `DependencyInjection.cs`.
   - Replace direct `DateTimeOffset.UtcNow` in touched workflow service/orchestrator paths.
   - Add a simple fixed test clock for unit tests.

6. Emit the queued event in `WorkflowService`.
   - After `CreateWorkflowAsync`, add a `WorkflowQueued` event.
   - Keep the existing `WorkflowLog`.
   - Add `ListEventsAsync`, `ListRunsAsync` returning `TaskRunResponse`, and `ListChatMessagesAsync`.

7. Add chat message inspection.
   - In `WorkflowService`, fetch the issue via `IWorkItemProvider.GetIssueAsync(workflow.IssueUrl, ...)`.
   - Return `WorkflowChatMessageResponse` from `WorkItem.UserComments`.
   - Include `id`, `author`, `body`, `url`, `updatedAt`.
   - Exclude automation comments using `PullRequestCommentMarkers.IsAutomationComment`.
   - Do not persist every chat body as workflow events.

8. Refactor orchestration state changes in `WorkflowOrchestrator`.
   - Add private helpers:
     - `TransitionWorkflowAsync`
     - `StartTaskRunAsync`
     - `AssignExternalJobAsync`
     - `CompleteTaskRunAsync`
     - `FailWorkflowAsync`
   - Replace inline status/timestamp mutations incrementally.
   - Each helper updates state and emits one structured event.
   - Keep existing logs until the UI fully uses events.

9. Capture task durations.
   - Set `StartedAt` only when a task first enters `Running`.
   - Set `CompletedAt` when a task reaches `Succeeded` or `Failed`.
   - Preserve `StartedAt` while polling running external jobs.
   - For immediate `CompletedResult` paths, set both timestamps deterministically from `IClock`.

10. Add actionable failure event details.
   - On agent failure, event `DetailsJson` should include `taskKind`, `externalId`, `failureReason`, and a short output excerpt.
   - On orchestration exception, include exception type and message.
   - Keep `Workflow.FailureReason` concise for list views.

11. Parse individual agent messages where practical.
   - Add a small parser/service that attempts to split known structured agent output into ordered messages.
   - Return parsed `AgentMessageResponse[]` on `TaskRunResponse`; if parsing fails, return an empty array and preserve raw `output`.
   - Test the parser with deterministic samples and malformed output.

12. Add stuck-run signals.
   - Add `WorkflowObservabilityOptions` with `RunningTaskStuckAfter` and `WorkflowStaleAfter`.
   - Add `WorkflowObservabilityService`.
   - Implement `GetWorkflowSignalsAsync(Guid workflowId, ...)`.
   - Detect running task older than threshold, non-terminal workflow with stale `UpdatedAt`, and running task missing `ExternalId`.
   - Return severity, reason, workflow id, optional task run id, and observed timestamp.

13. Expose API endpoints in `Program.cs`.
   - `GET /api/workflows/{workflowId}/events`
   - `GET /api/workflows/{workflowId}/signals`
   - `GET /api/workflows/{workflowId}/chat-messages`
   - Keep `/logs`.
   - Change `/runs` to return `TaskRunResponse`.

14. Update `ClientApp/src/api.ts`.
   - Add types and fetch functions for events, signals, chat messages, and agent messages.
   - Add `startedAt`, `completedAt`, and parsed message fields to task runs.

15. Update `ClientApp/src/App.tsx`.
   - Load workflow, runs, logs, events, signals, and chat messages together.
   - Add a chronological “Timeline” section from `/events`.
   - Show signals near the workflow summary.
   - Show task duration on task cards.
   - Add “Chat Messages”.
   - For task output, event details, log messages, chat bodies, and parsed agent messages, render collapsed by default with an expand/collapse button.

16. Update CSS.
   - Add reusable collapsed/expanded styles for prose and `pre` content.
   - Ensure long URLs, markdown-like text, JSON, and logs wrap safely.
   - Keep detail view compact by default.

17. Add deterministic tests.
   - `WorkflowServiceTests`: manual workflow records `WorkflowQueued`.
   - `WorkflowServiceTests`: chat messages exclude automation comments.
   - `WorkflowOrchestratorTests`: successful workflow records ordered transition/task events.
   - `WorkflowOrchestratorTests`: failed agent run records task failure, workflow failure, and useful event details.
   - `WorkflowOrchestratorTests`: deferred agent run preserves `StartedAt`; later completion sets `CompletedAt`.
   - Add signal tests with fixed clock.
   - Add agent message parser tests.

18. Verify.
   - Run `dotnet test`.
   - Run `npm run build` in `src/hhnl.Formicae.Api/ClientApp`.
   - Manually confirm events are chronological, signals report stale/stuck states, chat messages render, task durations display, and all long content is collapsed by default.
```

### Implement

Status: Succeeded

```text
Implemented the workflow observability plan.

Changed:
- Added workflow events, task timestamps, response DTOs, agent message parsing, clock abstraction, and observability signal service.
- Persisted `workflow_events` plus `task_runs.started_at/completed_at` via EF and in-memory stores.
- Emitted structured events from workflow/task lifecycle changes, including failure details.
- Added API endpoints for `/events`, `/signals`, and `/chat-messages`; `/runs` now returns enriched task responses.
- Updated React UI with Timeline, Signals, Chat Messages, durations, parsed agent messages, and collapsed long content.
- Added deterministic tests for queued events, chat filtering, parser behavior, signals, ordered events, failures, and deferred timestamps.
- Bumped version from `0.1.25` to `0.1.26` across project, Helm, and deployment docs.
- Rebuilt static frontend assets under `src/hhnl.Formicae.Api/wwwroot`.

Verification:
- `dotnet test --no-restore` passed: 54 tests.
- `npm run build` passed in `src/hhnl.Formicae.Api/ClientApp`.

Note: `dotnet-ef` is not installed in this environment, so the EF migration was hand-authored and validated by compile/tests rather than generated by the EF CLI.
```

### CreatePullRequest

Status: Running

```text

```
