# Formicae Workflow

- Workflow: `92ce2efa846c4207bbe19b86646becef`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/46
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text
## Implementation Plan

1. Confirm scope and current behavior
   - Work in `src/hhnl.Formicae.Api/ClientApp`.
   - Keep existing `activePage` state and `?page=` query-param behavior unless the issue owner clarifies that real routes are required.
   - Preserve permission gating: workflow definitions require `canViewWorkflows`; integrations, repositories, and settings require `canAdminister`.

2. Extract page rendering into small helpers
   - In `src/App.tsx`, replace the long inline `activePage === ...` JSX block with a `renderActivePage()` helper.
   - Keep existing page components and props unchanged.
   - This makes the shell/navigation change easier to test without changing page behavior.

3. Add a reusable side navigation model
   - Define a local `navigationItems` array with:
     - `page`
     - label
     - disabled condition
   - Use it for all navigation rendering instead of duplicating buttons.
   - On click, call a shared `navigateToPage(page)` function that sets `activePage` and closes the mobile drawer.

4. Replace the top navigation with a desktop side menu
   - Remove the `<nav className="app-menu">` from `.topbar`.
   - Add an app layout wrapper, for example:
     - `.app-layout`
     - `.side-nav`
     - `.app-content`
   - Render the Formicae brand, page links, account status, and app version inside the side nav.
   - Keep the page title and page-specific refresh button in a slimmer content header.

5. Add a mobile drawer
   - Add React state: `const [menuOpen, setMenuOpen] = useState(false);`
   - Add a mobile menu button in the content header.
   - On small screens, make `.side-nav` a fixed drawer that slides in/out.
   - Add a backdrop button/overlay that closes the drawer.
   - Close the drawer when:
     - a nav item is selected
     - the backdrop is clicked
     - viewport returns to desktop if needed, via CSS-only behavior or a small resize effect.

6. Update CSS responsively
   - In `src/styles.css`, replace `.topbar`, `.topbar-actions`, `.app-menu`, and mobile nav styles with side-nav layout styles.
   - Desktop:
     - `.app-shell` should become a full-height shell with no large outer padding.
     - side nav fixed-width, content scrolls normally.
   - Mobile:
     - drawer overlays content.
     - content keeps existing `14px` mobile padding.
     - ensure tables/panels still use existing responsive rules.

7. Preserve page refresh controls
   - Move the existing refresh button conditional into the content header.
   - Keep the same handlers:
     - workflows: `refreshWorkflows`
     - definitions: `refreshWorkflowDefinitions`
     - integrations: `refreshIntegrations`
     - repositories: `refreshAvailableRepositories`
     - users: `refreshCurrentUser`
   - Settings currently has no refresh button; keep that behavior.

8. Verify
   - Run:
     ```bash
     cd src/hhnl.Formicae.Api/ClientApp
     npm install
     npm run build
     ```
   - Manually check with `npm run dev`:
     - desktop shows side nav and no top nav links
     - mobile shows drawer button and drawer closes after navigation
     - disabled links remain disabled for unauthorized pages
     - existing `?page=repositories`, `?page=users`, etc. still select the correct page
     - account status/logout still works from the new side menu

9. Version bump
   - Because this is a source/UI feature, bump the project version consistently per `AGENTS.md`.
   - Update:
     - `Directory.Build.props`
     - `deploy/helm/formicae/Chart.yaml`
     - `deploy/helm/formicae/values.yaml`
   - Use a minor version bump unless the repository has an existing unreleased versioning convention that indicates otherwise.
```

### Implement

Status: Succeeded

```text
Implemented the workflow UI change.

Changed:
- [App.tsx](/workspace/repo/src/hhnl.Formicae.Api/ClientApp/src/App.tsx:206): added shared navigation model, drawer state, refresh helper, `renderActivePage()`, and side-nav shell.
- [styles.css](/workspace/repo/src/hhnl.Formicae.Api/ClientApp/src/styles.css:41): replaced top navigation styling with desktop side nav and mobile drawer/backdrop styles.
- Version bumped to `0.4.0` in [Directory.Build.props](/workspace/repo/Directory.Build.props:3), [Chart.yaml](/workspace/repo/deploy/helm/formicae/Chart.yaml:5), and [values.yaml](/workspace/repo/deploy/helm/formicae/values.yaml:6).
- `npm run build` regenerated tracked `src/hhnl.Formicae.Api/wwwroot` assets.

Verified:
- `npm install`
- `npm run build`
- `git diff --check`
- Vite dev server is running at `http://localhost:5173/`
- `curl -I http://localhost:5173/?page=repositories` returns `200 OK`

I could verify the dev entry point and build output here, but full account/logout behavior still depends on the API backend behind the Vite `/api` proxy.
```

### CreatePullRequest

Status: Running

```text

```
