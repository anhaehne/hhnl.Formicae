# Formicae Workflow

- Workflow: `797876ee28e5451997ce00b716aa47bb`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/2
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text

**Implementation Plan**

1. **Add a recent workflow list API**
   - Extend `IWorkflowStore` in `/workspace/hhnl.Formicae/src/hhnl.Formicae.Application/Workflows/Interfaces.cs` with:
     - `ListRecentWorkflowsAsync(int limit, CancellationToken cancellationToken)`
   - Implement it in:
     - `InMemoryWorkflowStore`: order by `CreatedAt` descending, cap to limit.
     - `EfWorkflowStore`: same query with `AsNoTracking()`.
   - Add `WorkflowService.ListRecentWorkflowsAsync(limit, ct)` returning `WorkflowSummaryResponse[]`.
   - Add minimal API endpoint in `Program.cs`:
     - `GET /api/workflows?limit=25`
     - Clamp limit, for example `1..100`.

2. **Add backend tests**
   - Add unit coverage in `tests/hhnl.Formicae.Tests` for:
     - Starting multiple workflows and listing newest first.
     - Limit is respected.
   - If endpoint-level tests are not already set up, keep tests at service/store level for this slice.

3. **Create the management UI project**
   - Add a Vite React TypeScript app under:
     - `/workspace/hhnl.Formicae/src/hhnl.Formicae.Api/ClientApp`
   - Keep dependencies minimal:
     - React
     - TypeScript
     - Vite
   - Build one screen at `/` with:
     - Manual trigger form.
     - Recent workflow runs table.
     - Selected workflow detail panel.

4. **Implement API client functions**
   - Add small typed client helpers in `ClientApp/src/api.ts`:
     - `listWorkflows(limit)`
     - `startWorkflow(request)`
     - `getWorkflow(workflowId)`
     - `listRuns(workflowId)`
     - `listLogs(workflowId)`
   - Use the existing API contracts:
     - `POST /api/workflows/github-issue`
     - `GET /api/workflows`
     - `GET /api/workflows/{workflowId}`
     - `GET /api/workflows/{workflowId}/runs`
     - `GET /api/workflows/{workflowId}/logs`

5. **Build the first operator workflow**
   - Manual trigger form fields:
     - Issue URL
     - Repository URL
     - Base branch, default `main`
     - Model, optional
   - On submit:
     - POST to the existing trigger endpoint.
     - Show validation/API errors inline.
     - Refresh recent workflows.
     - Select the newly created workflow.

6. **Build recent run inspection**
   - Recent workflow table columns:
     - Created time
     - Status
     - Current step
     - Issue URL
     - Pull request link, when present
     - Failure reason, when present
   - Selecting a workflow loads:
     - Task runs from `/runs`
     - Logs from `/logs`
   - Detail panel should show:
     - Workflow id
     - Status/current step
     - Pull request URL as an external link
     - Plan artifact if available from workflow response in future; for now show task run output/artifacts where available.
     - Logs as timestamped entries.

7. **Wire UI into ASP.NET Core**
   - Configure Vite build output to `wwwroot`.
   - Update `hhnl.Formicae.Api.csproj` so `ClientApp` installs/builds as part of publish, or document manual UI build for the first slice.
   - In `Program.cs`, add:
     - `app.UseDefaultFiles();`
     - `app.UseStaticFiles();`
     - Map API endpoints before fallback/static behavior.
   - For local development, allow running Vite separately with proxy to `http://localhost:5000`.

8. **Document local development**
   - Update `/workspace/hhnl.Formicae/README.md`:
     - Install UI dependencies.
     - Run API.
     - Run Vite dev server.
     - Open the management UI.
     - Explain fake adapters make the UI usable locally without external systems.
   - Add publish/build note if UI is bundled into API static files.

9. **Verification checklist**
   - `dotnet build hhnl.Formicae.slnx`
   - `dotnet test tests/hhnl.Formicae.Tests/hhnl.Formicae.Tests.csproj`
   - From `ClientApp`: `npm install`, `npm run build`
   - Manual smoke test:
     - Start API with fake adapters.
     - Open UI.
     - Submit a GitHub issue workflow.
     - Confirm it appears in recent workflows.
     - Select it and confirm logs/runs are visible after orchestration advances.

```

### Implement

Status: Succeeded

```text
Implemented the approved workflow slice.

Changed:
- Added `GET /api/workflows?limit=25` with limit clamped to `1..100`.
- Added recent workflow listing through `IWorkflowStore`, `WorkflowService`, in-memory store, and EF store with `AsNoTracking()`.
- Extended `WorkflowSummaryResponse` with `issueUrl` and `repositoryUrl` for the management UI.
- Added service/store tests for newest-first ordering and limit behavior.
- Added Vite React TypeScript app under `src/hhnl.Formicae.Api/ClientApp`.
- Added static built UI output under `src/hhnl.Formicae.Api/wwwroot`.
- Wired ASP.NET Core static file serving and SPA fallback.
- Updated README with API/UI local dev and build instructions.
- Added ignore rules for TypeScript build info and Docker `node_modules`.

Verification:
- `npm install`: passed.
- `npm run build` from `ClientApp`: passed.
- `dotnet build hhnl.Formicae.slnx`: not run, `dotnet` is not installed in this container.
- `dotnet test tests/hhnl.Formicae.Tests/hhnl.Formicae.Tests.csproj`: not run for the same reason.
```

### CreatePullRequest

Status: Running

```text

```
