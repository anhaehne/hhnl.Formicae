# Formicae Workflow

- Workflow: `67c6c54678f346819ec811de4db94bf7`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/24
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text
**Implementation Plan**

1. Add Octokit.NET dependency
   - Add `PackageReference Include="Octokit" Version="14.0.0"` to `src/hhnl.Formicae.Infrastructure/hhnl.Formicae.Infrastructure.csproj`.
   - Run `dotnet restore`.

2. Replace raw `HttpClient` GitHub wiring
   - In `DependencyInjection.cs`, register an authenticated `GitHubClient` or small factory instead of `AddHttpClient` for the GitHub providers.
   - Use `ProductHeaderValue("hhnl-formicae")`.
   - Read `GITHUB_TOKEN`; require it for source-control operations, keep work-item reads token-optional if preserving current behavior.

3. Refactor `GitHubWorkItemProvider`
   - Change constructor from `HttpClient` to `GitHubClient`.
   - Replace REST DTO calls with Octokit APIs:
     - `Issue.Get(owner, repo, number)`
     - `Issue.Comment.GetAllForIssue(owner, repo, number)`
     - `Issue.GetAllForRepository(owner, repo, RepositoryIssueRequest { State = ItemStateFilter.Open, Labels = { label } })`
     - `Issue.Comment.Create` / `Issue.Comment.Update`
     - `Reaction.Issue.Create`
   - Keep `GitHubIssueReference.Parse` and current `WorkItem` mapping behavior.

4. Refactor `GitHubSourceControlProvider`
   - Change constructor from `HttpClient` to `GitHubClient`.
   - Replace raw REST operations with Octokit APIs where available:
     - base ref: `Git.Reference.Get(owner, repo, $"heads/{baseBranch}")`
     - pull requests: `PullRequest.GetAllForRepository`, `PullRequest.Create`
     - PR issue comments: `Issue.Comment.GetAllForIssue`, `Issue.Comment.Create`
     - PR review comments: `PullRequest.ReviewComment.GetAll`
     - reactions: Octokit reaction clients for issue/review comments
     - workflow summary file: `Repository.Content.GetAllContentsByRef`, `CreateFile`, `UpdateFile`
   - Preserve existing id formats: `issue:{id}` and `review:{id}`.

5. Link created branches to issues
   - Change `ISourceControlProvider.CreateBranchAsync` to accept `issueUrl` or the full `Workflow`, because current parameters do not include the issue number.
   - Update callers in `WorkflowOrchestrator`, fake provider, and test doubles.
   - In `GitHubSourceControlProvider.CreateBranchAsync`, prefer GitHub’s linked-branch GraphQL mutation:
     - Query repository issue node id and base branch target oid.
     - Run `createLinkedBranch(input: { issueId, oid, name })`.
     - Return the created/ref branch name.
   - If the mutation reports the branch already exists, treat it as idempotent and return the intended branch name, matching today’s `422` behavior.
   - Note: GitHub documents linked issue branches as public preview, and GitHub’s changelog says GraphQL supports `createLinkedBranch`.

6. Keep PR issue linking behavior
   - Keep the PR body line `Issue: {workflow.IssueUrl}`.
   - Optionally change it to a closing/reference keyword only if product wants automatic issue closure; otherwise the linked branch should make the PR appear in the issue Development sidebar when the PR is opened from that branch.

7. Add focused tests
   - Add unit tests for `GitHubSourceControlProvider.CreateBranchAsync` using a fake Octokit connection/handler or an extracted `IGitHubClient` wrapper.
   - Assert it sends/uses `createLinkedBranch` with:
     - parsed owner/repo/issue number
     - branch name `formicae/{workflowId:N}`
     - base branch oid
   - Add an idempotency test for existing linked branch / existing ref behavior.
   - Update existing orchestrator tests for the new `CreateBranchAsync` signature and assert the issue URL is passed through.

8. Verify
   - Run `dotnet test hhnl.Formicae.slnx`.
   - If available, run one manual integration check with `UseFakeAdapters=false`, `GITHUB_TOKEN`, and a test issue labeled `ready-to-implement`; confirm the branch appears under the issue’s Development section.

Sources: Octokit.NET repo/package docs: https://github.com/octokit/octokit.net, GitHub linked-branch changelog: https://github.blog/changelog/2022-10-31-graphql-apis-for-creating-a-branch-linked-to-an-issue/, GitHub issue branch docs: https://docs.github.com/en/issues/tracking-your-work-with-issues/using-issues/creating-a-branch-for-an-issue
```

### Implement

Status: Succeeded

```text
Implemented the Octokit migration and linked-branch workflow.

Changed:
- Added `Octokit` `14.0.0` and registered authenticated `GitHubClient` instances in DI.
- Refactored GitHub work-item and source-control providers off raw `HttpClient`.
- Added `createLinkedBranch` GraphQL support via `OctokitGitHubApi`.
- Updated `ISourceControlProvider.CreateBranchAsync` to receive `issueUrl`; orchestrator and fakes now pass it through.
- Added focused tests for linked branch creation/idempotency and updated GitHub provider tests.
- Bumped version alignment to `0.1.21` in project, Helm chart/values, and deployment docs.

Verification:
- `dotnet restore hhnl.Formicae.slnx`
- `dotnet test hhnl.Formicae.slnx` passed: 45 tests.

Manual GitHub integration was not run because it requires a real `GITHUB_TOKEN` and test issue.
```

### CreatePullRequest

Status: Running

```text

```
