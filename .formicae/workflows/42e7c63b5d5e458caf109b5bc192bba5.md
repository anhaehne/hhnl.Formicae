# Formicae Workflow

- Workflow: `42e7c63b5d5e458caf109b5bc192bba5`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/35
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text
Implementation plan for issue #35: https://github.com/anhaehne/hhnl.Formicae/issues/35

1. Add regression coverage in `tests/hhnl.Formicae.Tests/WorkflowOrchestratorTests.cs`.

2. Extend an existing first-planning test, such as `AdvanceRunnableWorkflows_Waits_for_ready_to_implement_label_after_planning`, to assert:
   - `devOps.UpsertIssueCommentCalls` has exactly one plan comment.
   - `devOps.AddIssueCommentCalls` is empty.

3. Add a focused deferred-run regression test:
   - Start a queued issue workflow with `DeferredAgentRunner`.
   - Advance once so the plan task is `Running`.
   - Simulate streamed/partial agent output on the running `TaskRun.Output`, either by using `WorkerAgentMessageService.RecordAsync` or directly updating the run.
   - Complete the deferred plan result and advance again.
   - Assert the plan comment was upserted once and no revision summary comment was added.

4. Keep the existing revision test `AdvanceRunnableWorkflows_Revises_plan_when_issue_comment_is_newer_than_plan` as the positive case proving the second comment still appears after user feedback.

5. Update `src/hhnl.Formicae.Application/Workflows/WorkflowOrchestrator.cs`.
   - In the `existing?.Status == TaskRunStatus.Running` branch of `RunPlanningAsync`, stop using `existing.Output` as the revision signal.
   - Use a stable “prior completed plan exists” signal instead, preferably `workflow.PlanArtifact`, because `TaskRun.Output` can contain live streamed output from the current first planning run.
   - Pass that boolean into `CompleteSuccessfulPlanningAsync`.

6. In the `forceRefresh` path of `RunPlanningAsync`, ensure the previous successful plan is preserved before scheduling a revision:
   - Capture `run.Output` before restarting the plan run.
   - Set `workflow.PlanArtifact` from that previous output when needed.
   - Persist the workflow if this value is newly assigned, so a deferred revision still knows it is a revision on the next tick.

7. Run the targeted test suite:

```bash
dotnet test tests/hhnl.Formicae.Tests/hhnl.Formicae.Tests.csproj
```

8. Confirm expected behavior:
   - First planning iteration creates only the upserted plan comment.
   - First planning iteration with streamed output still creates no revision summary comment.
   - Planning after newer user issue feedback still upserts the plan and adds the revision summary comment.
```

### Implement

Status: Succeeded

```text
Implemented issue #35.

Changed:
- [WorkflowOrchestrator.cs](/workspace/repo/src/hhnl.Formicae.Application/Workflows/WorkflowOrchestrator.cs:101): running plan completion now uses `workflow.PlanArtifact` to decide whether it is a revision, so streamed output from a first run no longer triggers a revision summary comment.
- [WorkflowOrchestrator.cs](/workspace/repo/src/hhnl.Formicae.Application/Workflows/WorkflowOrchestrator.cs:127): force-refresh planning preserves the previous completed plan artifact before restarting.
- [WorkflowOrchestratorTests.cs](/workspace/repo/tests/hhnl.Formicae.Tests/WorkflowOrchestratorTests.cs:397): first planning now asserts one upserted plan comment and no added revision comment.
- [WorkflowOrchestratorTests.cs](/workspace/repo/tests/hhnl.Formicae.Tests/WorkflowOrchestratorTests.cs:461): added deferred first-plan regression with streamed output.
- Bumped version metadata to `0.3.22` in [Directory.Build.props](/workspace/repo/Directory.Build.props:3), [Chart.yaml](/workspace/repo/deploy/helm/formicae/Chart.yaml:5), and [values.yaml](/workspace/repo/deploy/helm/formicae/values.yaml:6).

Verification:
`dotnet test tests/hhnl.Formicae.Tests/hhnl.Formicae.Tests.csproj`

Result: passed, `104` tests.
```

### CreatePullRequest

Status: Running

```text

```
