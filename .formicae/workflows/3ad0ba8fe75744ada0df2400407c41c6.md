# Formicae Workflow

- Workflow: `3ad0ba8fe75744ada0df2400407c41c6`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/13
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text
## Changes from previous plan

- Generalize triggers from GitHub-only to DevOps provider triggers so GitHub and Gitea share one workflow trigger path.
- Replace `GitHubIssueLabel` with a provider-neutral `DevOpsIssueLabel` trigger type, still backed by `ConnectedRepository` records.
- Populate the repository multi-select from all connected repositories, grouped/labeled by provider and integration.
- Extend both `GitHubWebhookHandler` and `GiteaWebhookHandler` to emit the same trigger event DTO after signature validation.

## Implementation Plan

1. **Add trigger definition models**
   - In `src/hhnl.Formicae.Application/Workflows/WorkflowModels.cs`, add:
     - `WorkflowTriggerType` enum: `Manual`, `DevOpsIssueLabel`
     - `WorkflowDefinitionTrigger` record with `Id`, `Type`, `Enabled`, `RepositoryIds`, `Label`, optional `BaseBranch`, optional `Model`
   - Extend `WorkflowDefinitionDocument` with optional `Triggers`.
   - Keep `Triggers` nullable or empty so old definitions remain valid.

2. **Preserve JSON compatibility**
   - Update `WorkflowDefinitionJson` only as needed so old workflow definitions deserialize with `Triggers == null` or `[]`.
   - Do not add default triggers in `DefaultWorkflowDefinitions.CreateMvp`.
   - Add a test proving an old definition without triggers still deserializes and validates.

3. **Validate trigger shape**
   - Extend `WorkflowDefinitionValidator`:
     - allow missing/empty triggers
     - require unique non-empty trigger `Id`
     - require known `Type`
     - for enabled `DevOpsIssueLabel`, require at least one `RepositoryId` and non-empty `Label`
   - In `WorkflowDefinitionService.CreateVersionAsync`, after syntactic validation, use `IDevOpsIntegrationStore.ListAllRepositoriesAsync()` to reject unknown repository IDs.
   - Update DI constructor usage and tests for `WorkflowDefinitionService`.

4. **Persist trigger audit events**
   - Add `WorkflowTriggerEvent` model with:
     - `Id`
     - `WorkflowId`
     - `WorkflowDefinitionId`
     - `WorkflowDefinitionVersionId`
     - `TriggerId`
     - `TriggerType`
     - `Provider`
     - `ExternalDeliveryId`
     - `EventName`
     - `Action`
     - `PayloadSummaryJson`
     - `CreatedAt`
   - Add `DbSet<WorkflowTriggerEvent>` and EF mappings in `FormicaeDbContext`.
   - Add indexes for:
     - `WorkflowId`
     - `CreatedAt`
     - `ExternalDeliveryId`
     - unique or lookup index on `ExternalDeliveryId + TriggerId`
   - Add `IWorkflowStore` methods:
     - `AddTriggerEventAsync`
     - `ListTriggerEventsAsync(Guid workflowId, ...)`
     - `GetTriggerEventByDeliveryAsync(string deliveryId, string triggerId, ...)`
   - Implement in `EfWorkflowStore` and `InMemoryWorkflowStore`.
   - Add EF migration and snapshot update.

5. **Create provider-neutral trigger service**
   - Add `WorkflowTriggerService` in Application.
   - Inject `IWorkflowStore`, `IDevOpsIntegrationStore`, `WorkflowService`, `IClock`.
   - Add DTO:
     - `DevOpsIssueLabelTriggerEvent`
     - fields: `ProviderType`, `DeliveryId`, `EventName`, `Action`, `RepositoryUrl`, `IssueUrl`, `Label`, optional `RepositoryFullName`
   - Add `HandleIssueLabelEventAsync(...)`.
   - Behavior:
     - ignore anything except `issues/labeled`
     - load enabled workflow definition versions
     - inspect enabled `DevOpsIssueLabel` triggers
     - resolve configured `RepositoryIds` through `IDevOpsIntegrationStore.ListAllRepositoriesAsync()`
     - match repository URL case-insensitively
     - match label case-insensitively
     - skip if `ExternalDeliveryId + TriggerId` already audited
     - skip if `GetWorkflowByIssueUrlAsync(issueUrl)` already exists
     - start workflow with `WorkflowService.StartGitHubIssueWorkflowAsync`
     - use trigger `BaseBranch` if present, otherwise selected repository `DefaultBranch`
     - use trigger `Model` if present
     - write `WorkflowTriggerEvent` linked to the started workflow
   - Keep the existing method name `StartGitHubIssueWorkflowAsync` for now to avoid broad renaming; treat it as the existing issue-workflow start API.

6. **Wire GitHub webhooks**
   - Extend `GitHubWebhookHandler` envelope records to include:
     - repository `html_url`, `full_name`
     - issue `html_url`
     - issue labels if needed
     - labeled label name from payload `label.name`
   - Inject `WorkflowTriggerService`.
   - After signature validation and JSON parsing, call `HandleIssueLabelEventAsync` for `issues/labeled`.
   - Preserve existing PR completion and requeue behavior via `DevOpsWebhookProcessor`.
   - Signal `WorkflowTickNotifier` when a workflow is started, completed, or requeued.
   - Return accepted payload with `startedWorkflowIds` or `startedWorkflowCount`.

7. **Wire Gitea webhooks**
   - Extend `GiteaWebhookHandler` envelope records similarly:
     - repository `html_url` or equivalent clone/html URL
     - repository full name
     - issue `html_url`
     - labeled label name
   - Inject `WorkflowTriggerService`.
   - After Gitea signature validation, call the same `HandleIssueLabelEventAsync` for `issues/labeled`.
   - Preserve existing PR completion and requeue behavior.
   - Signal `WorkflowTickNotifier` when a workflow is started, completed, or requeued.
   - Return accepted payload with started workflow information.

8. **Keep manual starts intact**
   - Do not change the existing manual API request/response contract for `POST /api/workflows/github-issue`.
   - Manual start must still resolve the default enabled workflow definition when no definition/version is specified.
   - Add or keep a test proving manual start works when definitions have no triggers.

9. **Add workflow definition UI support**
   - Update `src/hhnl.Formicae.Api/ClientApp/src/api.ts` workflow definition types to include `triggers`.
   - In `WorkflowDefinitionsPage.tsx`, add a compact “Triggers” editor:
     - add/remove trigger rows
     - enabled checkbox
     - type select with `DevOpsIssueLabel`
     - label input
     - repository multi-select populated from connected repositories
     - group repository options by provider/integration
     - optional base branch/model fields
   - Reuse existing integration/repository API data where possible.
   - Add client validation for label and at least one repository.

10. **Add tests**
   - `WorkflowDefinitionTests`:
     - old definitions without triggers validate
     - valid `DevOpsIssueLabel` validates
     - missing label fails
     - missing repositories fails
     - unknown repository ID fails during save
   - New `WorkflowTriggerServiceTests`:
     - GitHub `issues/labeled` starts one workflow
     - Gitea `issues/labeled` starts one workflow
     - audit event references workflow ID
     - duplicate delivery/trigger does not start another workflow
     - duplicate issue URL does not start another workflow
     - nonmatching label does nothing
     - nonmatching repository does nothing
     - repository default branch is used
     - trigger base branch overrides repository default branch
   - `GitHubWebhookHandlerTests`:
     - valid `issues/labeled` delegates after signature validation
     - existing PR completion/requeue tests still pass
   - `GiteaWebhookHandlerTests`:
     - valid `issues/labeled` delegates after signature validation
     - existing PR completion/requeue tests still pass
   - `WorkflowServiceTests`:
     - manual start remains compatible without triggers.

11. **Docs**
   - Update `docs/mvp-workflow.md` and `README.md`:
     - manual start remains `POST /api/workflows/github-issue`
     - issue-label triggers work for GitHub and Gitea connected repositories
     - repositories must be selected from integration-managed `ConnectedRepository` records
     - trigger events are audited and linked to workflow runs.

12. **Verification**
   - Run:
     - `dotnet build hhnl.Formicae.slnx`
     - `dotnet test tests/hhnl.Formicae.Tests/hhnl.Formicae.Tests.csproj`
   - If frontend changed:
     - `cd src/hhnl.Formicae.Api/ClientApp`
     - `npm install` if dependencies are missing
     - `npm run build`
   - Confirm the EF migration compiles and the model snapshot includes `WorkflowTriggerEvents`.
```

### Implement

Status: Succeeded

```text
Implemented the approved workflow trigger plan.

Key changes:
- Added provider-neutral workflow triggers: `WorkflowTriggerType.DevOpsIssueLabel`, trigger document model, validation, and repository ID save checks.
- Added `WorkflowTriggerService` for GitHub/Gitea `issues/labeled` events with duplicate delivery and duplicate issue guards.
- Added audited `WorkflowTriggerEvent` persistence with EF mapping, store methods, in-memory support, and generated migration `20260709152649_AddWorkflowTriggerEvents`.
- Wired GitHub and Gitea webhook handlers to start workflows from signed issue-label payloads while preserving PR completion/requeue behavior.
- Added trigger editor support in the workflow definition UI, including repository multi-select grouped by integration/provider.
- Updated docs and bumped version files from `0.5.0` to `0.6.0`.
- Regenerated frontend static assets under `src/hhnl.Formicae.Api/wwwroot`.

Tests:
- Added 15 test cases.
- Removed 0 tests.
- Edited 3 existing test files and added 1 new test file.

Verification passed:
- `dotnet build hhnl.Formicae.slnx` with 0 warnings
- `dotnet test tests/hhnl.Formicae.Tests/hhnl.Formicae.Tests.csproj` passed: 201 total
- `npm install`
- `npm run build`
- `git diff --check`

Note: `dotnet-ef` was not installed, so I installed EF tool `10.0.4` into `/tmp/dotnet-tools` only to generate the migration.
```

### CreatePullRequest

Status: Running

```text

```
