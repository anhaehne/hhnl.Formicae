# Formicae Workflow

- Workflow: `692fed414db2460fb40735d97848ad75`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/58
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text
## Implementation Plan

1. Confirm the repo is checked out on `main` before making changes:
   `git clone --branch main https://github.com/anhaehne/hhnl.Formicae.git`
   or, if already present, `git status --short --branch`.

2. Fix planning checkout branch selection in `src/hhnl.Formicae.Application/Workflows/WorkflowOrchestrator.cs`.
   In `RunPlanningAsync`, change the branch passed to `AgentTask` from the generated workflow branch:
   `workflow.BranchName ?? $"formicae/{workflow.Id:N}"`
   to `workflow.BaseBranch`.
   This keeps planning on the existing base branch so the worker can clone and checkout real code before producing a plan.

3. Leave implementation and PR comment handling behavior unchanged.
   `RunImplementationAsync` should still create/use `workflow.BranchName`.
   `AddressPullRequestCommentsAsync` should still require `workflow.BranchName`.
   The worker already checks out code for `Plan`, `Implement`, and `AddressComments` via `WorkerEnvironment.RequiresRepositoryCheckout`.

4. Add focused tests in `tests/hhnl.Formicae.Tests/WorkflowOrchestratorTests.cs`.
   Add or extend an orchestrator test using `DeferredAgentRunner` to assert:
   - planning starts an `AgentTask` with `TaskRunKind.Plan`
   - `AgentTask.BranchName` equals the workflow `BaseBranch`, for example `develop`
   - `workflow.BranchName` remains null after planning starts
   Also keep existing coverage that implementation creates and uses `formicae/{workflowId:N}`.

5. Optionally add a small regression assertion for comment handling if an existing test already captures `AddressComments` tasks:
   assert `TaskRunKind.AddressComments` still receives the persisted workflow branch, not `BaseBranch`.

6. Run verification:
   `dotnet test hhnl.Formicae.slnx`

7. Manual sanity check after tests:
   confirm planning jobs now receive `FORMICAE_BRANCH=<base branch>` through `OpenHandsAgentRunner`, while implementation/comment-address jobs continue to receive the workflow branch.
```

### Implement

Status: Succeeded

```text
Implemented the workflow branch fix.

Changed:
- [WorkflowOrchestrator.cs](/workspace/repo/src/hhnl.Formicae.Application/Workflows/WorkflowOrchestrator.cs:155): planning now starts `AgentTask` on `workflow.BaseBranch` instead of generating/using the workflow branch.
- [WorkflowOrchestratorTests.cs](/workspace/repo/tests/hhnl.Formicae.Tests/WorkflowOrchestratorTests.cs:348): added a focused regression test proving planning uses `develop` and leaves `workflow.BranchName` null.
- [WorkflowOrchestratorTests.cs](/workspace/repo/tests/hhnl.Formicae.Tests/WorkflowOrchestratorTests.cs:1022): strengthened address-comment coverage to assert it still uses the persisted workflow branch.

Verification:
- `dotnet test hhnl.Formicae.slnx` passed: 186 passed, 0 failed.

Test count: 1 test added, 1 existing test edited, 0 removed.
```

### CreatePullRequest

Status: Running

```text

```
