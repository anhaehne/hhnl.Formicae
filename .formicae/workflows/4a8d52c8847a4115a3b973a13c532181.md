# Formicae Workflow

- Workflow: `4a8d52c8847a4115a3b973a13c532181`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/6
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text
## Changes from previous plan

- Incorporates the OpenHands Canvas pattern: a `Settings > LLM` style panel with basic provider/model/auth fields and an advanced endpoint/base URL field.
- Keeps this issue to one active AI configuration, not full multi-profile support.
- Makes secret handling stricter: persist and display secret name/status only; do not expose API key values. A write-only API key editor should be deferred unless a Kubernetes secret writer is explicitly added.
- Clarifies “changes apply to new workflow executions only,” matching OpenHands’ model-setting behavior for new conversations.

## Implementation Plan

1. Baseline the repo
   - Run `dotnet test hhnl.Formicae.slnx`.
   - From `src/hhnl.Formicae.Api/ClientApp`, run `npm install` if needed, then `npm run build`.
   - Record any pre-existing failures before edits.

2. Add AI settings contracts
   - In `src/hhnl.Formicae.Application/Workflows/WorkflowModels.cs`, add:
     - `AiSettingsResponse`
     - `UpdateAiSettingsRequest`
   - Include only non-secret fields:
     - `Provider`
     - `Model`
     - `EndpointUrl`
     - `AuthMethod`
     - `LlmApiKeySecretName`
     - `HasApiKeySecret`
   - Do not include an API key value in any response or persisted DTO.

3. Add persistent settings model
   - Add `AiSettings` under `src/hhnl.Formicae.Application/Workflows`.
   - Use singleton row id `"default"`.
   - Fields:
     - `Id`
     - `Provider`
     - `Model`
     - `EndpointUrl`
     - `AuthMethod`
     - `LlmApiKeySecretName`
     - `UpdatedAt`
   - Add focused `IAiSettingsStore` to `Interfaces.cs`.

4. Implement settings storage
   - Add `DbSet<AiSettings>` and table config in `FormicaeDbContext`.
   - Implement `IAiSettingsStore` in EF, either in `EfWorkflowStore` or a new `EfAiSettingsStore`.
   - Implement in-memory storage in `InMemoryWorkflowStore` or a new `InMemoryAiSettingsStore`.
   - Register the store in `DependencyInjection.cs` for fake, in-memory, and Postgres modes.
   - Add EF migration for the `ai_settings` table.

5. Add `AiSettingsService`
   - Read persisted settings and merge with config defaults from:
     - `OpenHandsOptions.AuthMethod`
     - `OpenHandsOptions.DefaultModel`
     - `OpenHandsOptions.LlmApiKeySecretName`
     - new optional `OpenHandsOptions.Provider`
     - new optional `OpenHandsOptions.EndpointUrl`
   - Validate:
     - `AuthMethod` is `ApiKey` or `CodexSubscription`.
     - URL is empty or absolute.
     - strings are trimmed.
   - Return `HasApiKeySecret` based on non-empty `LlmApiKeySecretName`, not secret contents.

6. Extend options
   - Add optional `Provider` and `EndpointUrl` to `OpenHandsOptions`.
   - Add config defaults in:
     - `appsettings.json`
     - `deploy/kubernetes/base/configmap.yaml`
     - Helm `values.yaml`
     - Helm configmap template
   - Keep existing ConfigMap values as bootstrap defaults only.

7. Add API endpoints
   - In `Program.cs`, add:
     - `GET /api/ai-settings`
     - `PUT /api/ai-settings`
   - Use `AiSettingsService`.
   - Return `400` with `{ error }` for validation failures.
   - Ensure GET/PUT responses never contain API key material.

8. Use settings for manual workflow creation
   - Inject `AiSettingsService` into `WorkflowService`.
   - In `StartGitHubIssueWorkflowAsync`, if request `Model` is blank, use current AI settings `Model`.
   - Preserve explicit manual model override.

9. Use settings for discovery
   - Inject `AiSettingsService` into `WorkflowDiscoveryService`.
   - When creating new discovered workflows, prefer saved AI settings `Model`; fallback to `WorkflowDiscoveryOptions.Model`.
   - Leave repository, branch, and label discovery behavior unchanged.

10. Use settings in runner execution
   - Inject `AiSettingsService` or a read-only resolver into `OpenHandsAgentRunner`.
   - For each new job, resolve current settings before building `KubernetesJobSpec`.
   - Apply:
     - `FORMICAE_MODEL`
     - `LLM_MODEL` for `ApiKey`
     - `LLM_BASE_URL` when `EndpointUrl` is set
     - selected `AuthMethod`
   - Preserve fallback behavior when no settings row exists.
   - Ensure already-created/running jobs are unaffected.

11. Update frontend API client
   - In `ClientApp/src/api.ts`, add:
     - `AiSettings`
     - `UpdateAiSettingsRequest`
     - `getAiSettings()`
     - `updateAiSettings()`

12. Add UI panel
   - In `ClientApp/src/App.tsx`, add an “AI Settings” panel near manual trigger.
   - Follow OpenHands-inspired structure:
     - Basic: provider, model, auth method, API key secret name/status.
     - Advanced: endpoint/base URL.
   - Use a select for `AuthMethod`.
   - Show API key as configured/not configured only.
   - Save inline, keep returned settings in state, and display save errors inline.
   - Do not keep API key values in React state.

13. Prefill manual model
   - When AI settings load, prefill the manual trigger model only if the user has not typed one.
   - Keep the manual model field as an explicit per-workflow override.

14. Add tests
   - Backend:
     - GET returns non-secret settings.
     - PUT persists non-secret settings.
     - responses never include API key material.
     - manual workflows use saved model when request model is blank.
     - manual workflow model override still wins.
     - discovered workflows use latest saved model.
     - runner emits model and endpoint env vars for new jobs.
     - auth method validation rejects unsupported values.
   - Frontend:
     - `npm run build`.
   - Full:
     - `dotnet test hhnl.Formicae.slnx`.

15. Update docs
   - In `docs/kubernetes-deployment.md`, document:
     - AI settings are editable from the UI.
     - non-secret settings are persisted in the database.
     - API keys remain in Kubernetes secrets and are never shown in clear text.
     - changes apply to newly queued/newly started workflow executions only.
     - full OpenHands-style multi-profile switching is out of scope for this issue.

References used: OpenHands Agent Canvas setup and LLM settings docs describe configuring provider/model/API key in `Settings > LLM`, advanced base URL/custom model settings, and applying changes to new conversations.
```

### Implement

Status: Succeeded

```text
Implemented the AI settings workflow end to end.

Changed:
- Added AI settings contracts, persistent model, `AiSettingsService`, EF/in-memory stores, and `ai_settings` migration.
- Added `GET /api/ai-settings` and `PUT /api/ai-settings`.
- Wired saved model/settings into manual workflows, discovery, and new OpenHands runner jobs, including `FORMICAE_MODEL`, `LLM_MODEL`, and optional `LLM_BASE_URL`.
- Added the React “AI Settings” panel with provider/model/auth/secret status/advanced endpoint fields, without storing API key values.
- Updated config defaults, Kubernetes/Helm config, docs, built static assets, and bumped version to `0.1.28`.
- Added focused backend tests for settings persistence, validation, workflow model defaults/overrides, discovery model use, and runner env vars.

Verification:
- `npm run build` passed.
- `dotnet test hhnl.Formicae.slnx` passed: 61 tests.
```

### CreatePullRequest

Status: Running

```text

```
