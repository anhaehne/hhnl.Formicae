# Formicae Workflow

- Workflow: `ad3e98168057400db63da3c64432f635`
- Issue: https://github.com/anhaehne/hhnl.Formicae/issues/48
- Status: CreatingPullRequest

## Task Runs

### Plan

Status: Succeeded

```text
**Implementation Plan**

Issue source: https://github.com/anhaehne/hhnl.Formicae/issues/48

1. In `src/hhnl.Formicae.Api/ClientApp/src/App.tsx`, update `handleInstallGitHubApp`.
   - Keep the existing guard that shows `setRepositoryError(...)` when no installation URL is available.
   - Replace:
     ```ts
     window.location.href = integrationDetail.setupInstructions.installationUrl;
     ```
     with a new-tab open, preferably:
     ```ts
     window.open(integrationDetail.setupInstructions.installationUrl, "_blank", "noopener,noreferrer");
     ```
   - Do not change the disabled logic on the `Install GitHub App` button.

2. Build the frontend to catch TypeScript or Vite regressions:
   ```bash
   cd src/hhnl.Formicae.Api/ClientApp
   npm run build
   ```

3. Manually verify in the UI:
   - Start the app as usual.
   - Go to `Repositories`.
   - Select a GitHub integration that has an installation URL.
   - Click `Install GitHub App`.
   - Confirm GitHub opens in a new browser tab and the Formicae page remains on the repositories page.

4. Optional safety check:
   - Confirm existing same-tab redirects still work for GitHub login flows, since only the GitHub App installation button behavior should change.
```

### Implement

Status: Succeeded

```text
Implemented the workflow change.

Changed:
- [App.tsx](/workspace/repo/src/hhnl.Formicae.Api/ClientApp/src/App.tsx:985): `Install GitHub App` now opens the installation URL in a new tab via `window.open(..., "_blank", "noopener,noreferrer")`.
- Rebuilt committed frontend assets under `src/hhnl.Formicae.Api/wwwroot/`.

Verification:
- Ran `npm ci` in `src/hhnl.Formicae.Api/ClientApp`.
- Ran `npm run build` successfully.
- Confirmed the GitHub login flow still uses same-tab `window.location.href` at [App.tsx](/workspace/repo/src/hhnl.Formicae.Api/ClientApp/src/App.tsx:1029).
- Did not manually verify against a live configured GitHub integration in the browser because that environment/session was not available here.
```

### CreatePullRequest

Status: Running

```text

```
