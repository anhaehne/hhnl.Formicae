# Workflow editor usability upgrade

1. Save specification, research, baseline screenshot and standards.
2. Add optional persisted editor positions/viewport and a non-persisting validation endpoint with node references. Preserve immutable definitions and execution compatibility.
3. Implement a viewport workspace, searchable workflow/version switchers, searchable six-type node catalog, contextual task insertion, named ports, meaningful summaries, inspector and workflow settings drawers.
4. Centralize editor state and bounded logical undo/redo. Protect explicit-save drafts from refresh/navigation; use supported router blocking. Preserve failed saves and definition IDs.
5. Add ELK named-port layout, selection/navigation controls, keyboard and pointer alternatives, read-only behavior and responsive drawers.
6. Validate backend, browser editing/recovery/permissions, representative large layouts, screenshots and worker development lifecycle. Version 0.11.0 once; report test counts. Publication is separate.
