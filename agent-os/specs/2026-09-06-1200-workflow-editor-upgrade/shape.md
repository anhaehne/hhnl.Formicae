# Decisions

Approved: complete usability upgrade; left-to-right; explicit Save Version and leave warnings; no autosave or recovery. Keep tasks, loops, triggers and CLI model discovery. No parallel execution, run inspection, collaboration or new execution types. Optional layout metadata lives in existing definition JSON; runtime ignores it. Use existing green visual language, accessible controls, progressive disclosure and application-owned undo history. New incomplete disabled versions remain saveable.
