# Standards for Agent Deadline Recovery

No current repository standard applies to this timeout-only recovery.

`database/ef-migrations` is intentionally excluded because the migration correction remains scoped to issue #62.
