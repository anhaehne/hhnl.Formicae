# Decisions

User selected fixed-count loops and start-only triggers. Control nodes must be first-class persisted steps, not separate configuration lists. Keep per-task AI settings and existing built-in prerequisites. Legacy versions and execution state must not be rewritten.
