# Standards

Follow agent-os/standards/database/ef-migrations.md: generate any required migration with dotnet ef; never hand-edit migration files or snapshots. No database model change is planned.

Follow AGENTS.md validation, scoped authorization, and single minor-version bump rules.
